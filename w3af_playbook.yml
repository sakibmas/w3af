---
- hosts: w3af
  become: yes
  vars:
   hostname: www
   domain: bergerbd.com
  tasks:
   - name: Create w3af configruration from template
     template:
      src: ./generic.j2
      dest: ./{{hostname}}-generic.w3f
...
